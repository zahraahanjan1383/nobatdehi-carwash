

# موارد کاربرد (Use Cases)

## 🎭 نقش‌ها (Actors)
- مشتری (Customer): رزرو نوبت، پرداخت هزینه، مشاهده وضعیت نوبت
- مدیر کارواش (Admin): مدیریت نوبت‌ها، تایید یا لغو رزروها، مشاهده گزارش‌ها
- سیستم پرداخت (Payment System): تایید تراکنش‌ها و ثبت وضعیت پرداخت

---

## 📌 Use Case 1: رزرو نوبت
- Actor: مشتری  
- هدف: انتخاب زمان مناسب برای شست‌وشوی خودرو  
- جریان اصلی:
  1. مشتری وارد سیستم می‌شود.
  2. زمان آزاد را انتخاب می‌کند.
  3. سیستم نوبت را ثبت می‌کند.
- جریان جایگزین: اگر زمان انتخابی پر باشد، سیستم پیام خطا نمایش می‌دهد.

---

## 📌 Use Case 2: پرداخت آنلاین
- Actor: مشتری، سیستم پرداخت  
- هدف: پرداخت هزینه خدمات کارواش  
- جریان اصلی:
  1. مشتری گزینه پرداخت را انتخاب می‌کند.
  2. سیستم پرداخت تراکنش را تایید می‌کند.
  3. وضعیت نوبت به "پرداخت شده" تغییر می‌کند.
- جریان جایگزین: اگر پرداخت ناموفق باشد، سیستم پیام خطا نمایش می‌دهد.

---

## 📌 Use Case 3: مدیریت نوبت‌ها
- Actor: مدیر کارواش  
- هدف: مشاهده و مدیریت نوبت‌های ثبت‌شده  
- جریان اصلی:
  1. مدیر وارد پنل مدیریت می‌شود.
  2. لیست نوبت‌ها را مشاهده می‌کند.
  3. نوبت‌ها را تایید یا لغو می‌کند.

